var assert = require('assert');
var Space = require('../../../lib/game/space/space');

describe('Space', function () {
  it('can be imported', function () {
    assert.ok(Space, 'can not be imported');
  });
});
